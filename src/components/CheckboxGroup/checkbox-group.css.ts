import { css } from 'lit'

export default css`
  .group {
    border: 0;
    display: grid;
    gap: 8px;
    margin: 0;
    padding: 0;
  }

  .label {
    font-weight: 600;
    margin-bottom: 4px;
  }

  .option {
    align-items: center;
    display: flex;
    gap: 8px;
  }
`
